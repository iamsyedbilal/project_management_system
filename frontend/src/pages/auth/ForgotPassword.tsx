import { FormEvent,useState } from 'react'; import { Link } from 'react-router-dom'; import { forgotPassword } from '../../api/auth.api'
export default function ForgotPassword(){const [email,setEmail]=useState('');const [message,setMessage]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();try{const r=await forgotPassword(email);setMessage(r.message||'Password reset email requested.')}catch(err:any){setMessage(err.response?.data?.message||'Request failed')}};return <main className="auth-page"><form className="auth-card" onSubmit={submit}><h1>Forgot password</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button>Send reset request</button>{message&&<p>{message}</p>}<Link to="/login">Back to login</Link></form></main>}
